---
post_title: "fix(web): correct app.css asset path and render mode"
author1: mpaulosky
post_slug: "v0.0.11-pr-20"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.0.11
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.0.11 from PR #20."
post_date: "2026-09-11"
---
## fix(web): correct app.css asset path and render mode

- **Release tag:** `v0.0.11`
- **Source PR:** [#20](https://github.com/mpaulosky/TicketManager/pull/20)

### PR description

## Summary
- Fix `App.razor` to reference the stylesheet via `Assets["css/app.css"]` (matching its actual location under `wwwroot/css/`) instead of the broken `Assets["app.css"]` key.
- Add an explicit `@rendermode="InteractiveServer"` on `<Routes />`, matching `Program.cs`'s `AddInteractiveServerComponents()`/`AddInteractiveServerRenderMode()` registration.
- Update `WebApplicationTests.Get_Root_UsesTailwindStylesheetInsteadOfBootstrap` to assert on the stable `css/app` prefix instead of the literal `app.css` substring, since the corrected asset key now resolves to Blazor's fingerprinted static asset path (e.g. `css/app.<hash>.css`).

## Test plan
- [x] `dotnet build TicketManager.slnx` — 0 warnings, 0 errors
- [x] `dotnet test tests/Web.Tests.Integration/Web.Tests.Integration.csproj` — 3/3 passed
- [x] Full `pre-push` gate (YAML lint, Markdown lint, full test suite across all projects) passed on push

🤖 Generated with [Claude Code](https://claude.com/claude-code)

