---
post_title: "feat(web): migrate Bootstrap layout to Tailwind and rebrand shell"
author1: mpaulosky
post_slug: "v0.0.10-pr-19"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.0.10
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.0.10 from PR #19."
post_date: "2026-09-11"
---
## feat(web): migrate Bootstrap layout to Tailwind and rebrand shell

- **Release tag:** `v0.0.10`
- **Source PR:** [#19](https://github.com/mpaulosky/TicketManager/pull/19)

### PR description

## Summary
- Replaces the default Blazor Bootstrap scaffold (sidebar layout, Counter/Weather sample pages, Theme.css) with a Tailwind v4-based shell (NavMenu, MainLayout, Footer with GitHub release/commit metadata, theme toggle) and adds Auth0-aware login/redirect scaffolding.
- Fixes two build breaks left on the tree: missing `Web.Security.Auth0ConfigurationHelper` and an undefined `$(NpmCommand)` MSBuild property in `Web.csproj`.
- Fixes a broken GitHub URL in `Footer.razor` caused by a stray space in the repo slug (`"Ticket Manager"` → `"TicketManager"`).
- Updates three tests whose assertions still targeted the old Bootstrap/default-template markup, and adds unit coverage for the new `Auth0ConfigurationHelper`.

## Test plan
- [x] `dotnet restore`
- [x] `dotnet build TicketManager.slnx --no-restore` — 0 warnings, 0 errors
- [x] `dotnet test` per project — all 6 projects pass (AppHost.Tests, Architecture.Tests, Web.Tests.Bunit, Web.Tests.E2E, Web.Tests.Integration, Web.Tests.Unit)
- [x] `pre-push` hook gates (YAML lint, Markdown lint, full test suite) passed on push

🤖 Generated with [Claude Code](https://claude.com/claude-code)

