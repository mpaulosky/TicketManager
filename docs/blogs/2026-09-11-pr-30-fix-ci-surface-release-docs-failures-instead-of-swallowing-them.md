---
post_title: "fix(ci): surface release-docs failures instead of swallowing them"
author1: mpaulosky
post_slug: "v0.0.17-pr-30"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.0.17
  - automation
ai_note: "Generated by release automation using PR title/body"
summary: "Release notes seed for v0.0.17 from PR #30."
post_date: "2026-09-11"
---
## fix(ci): surface release-docs failures instead of swallowing them

- **Release tag:** `v0.0.17`
- **Source PR:** [#30](https://github.com/mpaulosky/TicketManager/pull/30)

### PR description

## Summary
- Removes `continue-on-error: true` from the release.yml docs-generation/commit step so a real failure shows as a red step, instead of being silently swallowed (this masked the RELEASE_PR_PAT gap for 13 releases)
- Keeps the release itself non-blocked: the "Create tag and release" step now runs on `!cancelled()` instead of implicit `success()`
- Adds a "Report docs generation failure" step that files (or comments on an existing) tracking issue when the docs step fails, so a future regression can't go unnoticed the same way

## Test plan
- [x] yamllint clean
- [x] Verified against the real failure this session reproduced (RELEASE_PR_PAT gap) before it was fixed — this change would have surfaced it as a red step + filed issue instead of a silently-green run

🤖 Generated with [Claude Code](https://claude.com/claude-code)

